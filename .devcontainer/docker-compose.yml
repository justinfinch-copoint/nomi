services:
  python-dev:
    image: mcr.microsoft.com/devcontainers/python:2-3.12-bullseye
    command: sleep infinity
    volumes:
      - ..:/workspace:cached
    # Uncomment to add additional services in the future
    # depends_on:
    #   - postgres
    #   - redis

  # Example: Add a PostgreSQL database service
  # postgres:
  #   image: postgres:15
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_DB: dev_db
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data

  # Example: Add Redis cache service
  # redis:
  #   image: redis:7-alpine
  #   restart: unless-stopped

# Uncomment when adding services that need persistent volumes
# volumes:
#   postgres-data:
